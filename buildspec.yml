version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18 # You can change this to 16, 20, etc.
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo No tests to run...
  post_build:
    commands:
      - echo Build complete

artifacts:
  files:
    - '**/*'